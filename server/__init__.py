# Server package 

# This is the main server that handles the WebRTC + WebTransport connection
# receives the video stream from the client and sends it to the client

# receives the ball coordinates from the client and sends it to the client
# receives the error feedback from the client and sends it to the client
